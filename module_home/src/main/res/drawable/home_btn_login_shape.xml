<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <stroke android:color="@android:color/white"
        android:width="@dimen/base_dimen_1"/>  <!-- 可以选择默认背景颜色 -->
    <corners android:radius="8dp"/> <!-- 设置圆角半径 -->
    <solid android:color="@android:color/transparent"/>
    <padding android:left="@dimen/base_dimen_8"
         android:right="@dimen/base_dimen_8"
        android:bottom="@dimen/base_dimen_6"
        android:top="@dimen/base_dimen_6"/>
</shape>