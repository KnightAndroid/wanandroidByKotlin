apply from: "${project.rootDir}/base_module.gradle"
android {
    resourcePrefix "web_"
    namespace = "com.knight.kotlin.module_web"
}

dependencies {
    implementation fileTree(dir: 'libs', include: ['*.jar'])
    implementation (libs.agentWeb)
    implementation (libs.sketch)
}

