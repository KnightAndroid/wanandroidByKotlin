//library-network配置文件

//apply from:'../base_library.gradle'
apply from: "${project.rootDir}/base_library.gradle"
android {
    buildFeatures {
        viewBinding = true
    }
    namespace = "com.knight.kotlin.library_common"
}

dependencies {
    implementation fileTree(dir: "libs", include: ["*.jar"])
    api project(path:':core:library_common')
    implementation (libs.hilt.android)
    kapt (libs.hilt.compiler)
    //kapt Dependencies.JetPack.HILT_ANDROIDX

    //百度地图
    api (libs.baidu.map.location)
    api (libs.baidu.map.allnavi)
    api (libs.baidu.map.search)
    api (libs.baidu.map.util)
    api  (libs.work.manager)
    api (libs.mmkv)


}