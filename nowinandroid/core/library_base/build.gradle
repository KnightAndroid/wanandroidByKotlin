//library-network配置文件

//apply from:'../base_library.gradle'
apply from: "${project.rootDir}/base_library.gradle"
android {
    buildFeatures {
        viewBinding = true
    }
    namespace = "com.knight.kotlin.library_base"
}

dependencies {
    implementation fileTree(dir: "libs", include: ["*.jar"])
    api project(path: ':core:library_base')
    api project(":nowinandroid:core:library_common")
    implementation (libs.hilt.android)
    kapt (libs.hilt.compiler)
    //kapt Dependencies.JetPack.HILT_ANDROIDX



}